---
import { SignIn } from "auth-astro/components";
import Layout from "../../layouts/Layout.astro";

import { getSession } from "auth-astro/server";
const session = await getSession(Astro.request);

if (session) {
  return Astro.redirect("/");
}

// const currentDomain =
//   import.meta.env.DEV
//     ? "http://localhost:4321"
//     : "https://mochi.meadow.cafe";

// const redirectUri = `${currentDomain}/user/auth_callback`;
---

<Layout title="Login">
  <div class="max-w-md mx-auto mt-10 bg-white p-8 rounded-lg shadow-md">
    <h1 class="text-2xl font-bold mb-6 text-center">Login</h1>
    <SignIn
      provider="github"
      class="mb-5 w-full inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
      >Github</SignIn
    >

    <!-- <p class="text-center text-gray-500 mb-6">
      This site uses IndieAuth to sign in. Enter your web address below to get
      started. If you need help setting up IndieAuth, check out this
      <a
        href="https://indielogin.com/setup"
        class="text-indigo-600 hover:underline">setup guide</a
      >.
    </p>
    <form action="https://indielogin.com/auth" method="get" class="space-y-6">
      <div>
        <label for="url" class="block text-sm font-medium text-gray-700"
          >Web Address:</label
        >
        <input
          id="url"
          type="text"
          name="me"
          placeholder="yourdomain.com"
          class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
        />
      </div>
      <div class="text-center">
        <button
          type="submit"
          class="w-full inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
          >Sign In</button
        >
      </div>
      <input type="hidden" name="client_id" value={currentDomain} />
      <input type="hidden" name="redirect_uri" value={redirectUri} />
      <input type="hidden" name="state" value="jwiusuerujs" />
    </form> -->
  </div>
</Layout>
